@model WebUI.Models.LevelModel
@{
    ViewData["Title"] = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main class="content">
    <div class="container-fluid p-0">
        <h1 class="h3 mb-3"><strong>Seviye</strong> Düzenleme Sayfası</h1>
        <div class="row">
            <div class="edit-table text-center">

                <div class="form-group row d-flex">
                    <label for="name" class="col-sm-4 col-form-label">İsmi</label>
                    <div class="col-sm-8">
                        <input class="form-dropdown text-center" type="text" placeholder="Örneğin:9">
                    </div>
                    <label for="Ders" class="col-sm-4 col-form-label">Durum</label>
                    <div class="col-sm-8">
                        <select class="form-dropdown">
                            <option value="">Aktif</option>
                            <option value="">Pasif</option>
                        </select>
                    </div>
                </div>
                <div>
                    <button id="cls_btn" class="upper_btn ">
                        <strong>Kaydet </strong><br>
                    </button>
                </div>


            </div>

        </div>
    </div>

</main>